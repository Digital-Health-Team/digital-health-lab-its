export interface Capability {
    tag: string;
    title: string;
    description: string;
    accent: string;
}

export interface AboutHeadlineWord {
    word: string;
    accent: boolean;
    lineBreakAfter: boolean;
}
